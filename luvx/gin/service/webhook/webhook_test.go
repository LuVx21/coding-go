package webhook

import (
	"testing"
)

func Test_sample_00(t *testing.T) {
	// fmt.Println(getAccessToken())
	// s, id := SendMessage(TO_USER, TEMPLATE_ID, "titletitle", map[string]WeixinMsg{"content": {Value: "测试消息github docker"}})
	// fmt.Println(s, id)
}
