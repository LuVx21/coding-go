BigCache

* 需要缓存数百万甚至上亿个条目（如全量用户会话、实时日志流）
* 对 GC 延迟极其敏感​ 的高频写入应用
* 数据有固定 TTL, 且可以接受“旧数据被新数据覆盖”的淘汰方式


Ristretto

* 数据库查询缓存: 需要极高的命中率来降低 DB 负载
* 热点数据缓存: 访问模式存在明显的“二八定律”（20%的键被访问80%的次数）
* 需要精细的内存控制（通过 Cost 区分大文件和小对象）
